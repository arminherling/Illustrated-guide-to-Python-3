# Exercise: Create a variable with your name stored as a string. Use indexing operations to pull
# out the first character. Pull out the last character. Will your code to pull out the last
# character work on a name of arbitrary length?

name = "Armin Herling"

print("Full name:", name)
print("First character:", name[0])
print("Last character:", name[-1])